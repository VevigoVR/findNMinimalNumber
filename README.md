# 📋 Описание проекта
N-th Minimum Finder - Spring Boot приложение для поиска N-ного минимального числа из Excel файлов. Проект реализует три алгоритма поиска:

🔍 Реализованные алгоритмы:
1. Основной метод - использует массив для хранения N минимальных чисел
2. Метод с пузырьковой сортировкой
3. Альтернативный метод (PriorityQueue) - более эффективный алгоритм с использованием кучи

🏗️ Архитектура:
Java 21 + Spring Boot 3

Docker контейнеризация

Swagger для документации API

Gradle для сборки

# 🚀 Инструкция по запуску

Способ 1: Автоматический запуск (рекомендуется)

Даем права на выполнение скрипта: chmod +x start.sh

Запускаем сборку и запуск: bash start.sh
#
Способ 2: Ручной запуск через Docker
Сборка и запуск: docker-compose up --build

Или по отдельности: 

docker-compose build

docker-compose up
#

Способ 3: Запуск без Docker: 

./gradlew clean build

./gradlew bootRun

# 📊 Использование API
Через Swagger UI:

Откройте: http://localhost:8083/swagger-ui.html

Найдите endpoint POST /api/n-min

Загрузите Excel файл и укажите параметр N

🔧 Структура проекта
text
n-min-finder/

├── src/main/java/com/example/finder/

│ │ ├── Application.java          # Главный класс

│ │ ├── NMinController.java       # REST контроллер

│ │ ├── NMinimumService.java      # Сервис с тремя алгоритмами

│ │ └── SwaggerConfig.java        # Конфигурация Swagger
│ │
│ └── test/java/com/example/finder/

│   ├── ExcelTestUtils            # Утилитный класс для тестирования

│   ├── NMinControllerTest        # Тестирование контроллера

│   └── NMinServiceTest           # Тестирование сервиса
│
├── test-data/                    # Тестовые файлы

├── docker-compose.yml            # Docker компоновка

├── Dockerfile                    # Конфигурация Docker

├── start.sh                      # Скрипт запуска

└── build.gradle.kts              # Конфигурация сборки

💻 Код сервиса с тремя методами
1. findNthSmallestFixed(int[] nums, int n)
2. findNthSmallestBasic(int[] nums, int n)
3. findNthSmallest(int[] nums, int n)

📈 Сравнение алгоритмов
Ручной (№ 1, № 2 методы)	Сложность O(m × n)	Память O(n)	Медленнее на больших данных
PriorityQueue (№ 3 метод) Сложность	O(m log n) Память O(n)	Высокая эффективность	Использует библиотечную коллекцию PriorityQueue

🐳 Docker команды для управления
bash
# Просмотр запущенных контейнеров
docker ps

# Просмотр логов
docker-compose logs -f

# Остановка приложения
docker-compose down

# Перезапуск
docker-compose restart

# Очистка системы Docker
docker system prune
🧪 Тестирование
Создайте тестовый файл test-data/numbers.xlsx:

text
| A  |
|----|
| 15 |
| 5  |
| 25 |
| 10 |
| 20 |
Запрос для N=3 должен вернуть: 15

🔄 Переключение между методами
Для использования PriorityQueue вместо ручного метода:

Откройте NMinimumService.java

в методе findNthMinimum(MultipartFile file, int n) раскомментируйте нужный return, закомментировав предыдущий
Пересоберите проект: bash start.sh

📝 Примечания
✅ Все алгоритмы соответствуют требованиям задания

✅ Не используются библиотечные функции сортировки массивов

✅ Реализована эффективная обработка больших файлов

✅ Проект полностью готов к продакшену

# Проект успешно запускается и готов к использованию! 🎉